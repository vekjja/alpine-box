apiVersion: v1
kind: Service
metadata:
  name: {{ include "alpine-box.fullname" . }}
  labels:
    {{- include "alpine-box.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alpine-box.selectorLabels" . | nindent 4 }}
